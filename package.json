{
  "name": "vimmer-io",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "copy-vim-wasm": "node scripts/copy-vim-wasm.mjs",
    "predev": "npm run copy-vim-wasm",
    "prebuild": "npm run copy-vim-wasm",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "dependencies": {
    "astro": "^6.2.1",
    "vim-wasm": "^0.0.13"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
